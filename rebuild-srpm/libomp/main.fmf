# Which component to build
test: COMPONENT=libomp ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_gcc
 - build_requires_perl
 - build_requires_cmake
 - build_requires_gnupg2
 - build_requires_elfutils
 - build_requires_libffi
 - build_requires_perl-Encode
 - build_requires_perl-Data-Dumper

recommend+:
- libomp
