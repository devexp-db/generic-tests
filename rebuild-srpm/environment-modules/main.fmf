# Which component to build
test: COMPONENT=environment-modules ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_dejagnu

recommend+:
 - environment-modules
