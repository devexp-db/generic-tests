# Which component to build
test: COMPONENT=galera ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_systemd
 - build_requires_openssl
 - build_requires_cmake
 - build_requires_check
 - build_requires_boost
 - build_requires_scons

recommend+:
- galera
