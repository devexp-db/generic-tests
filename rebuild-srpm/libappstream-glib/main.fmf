# Which component to build
test: COMPONENT=libappstream-glib ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_libarchive

recommend+:
 - libappstream-glib
