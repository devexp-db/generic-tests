# Which component to build
test: COMPONENT=golang ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_openssl
 - build_requires_perl
 - build_requires_golang
 - build_requires_pcre
 - build_requires_hostname

recommend+:
- golang
