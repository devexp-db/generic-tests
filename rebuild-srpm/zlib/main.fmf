# Which component to build
test: COMPONENT=zlib ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_make
 - build_requires_autoconf
 - build_requires_automake
 - build_requires_libtool

recommend+:
- zlib
