# Which component to build
test: COMPONENT=targetcli ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_python3.9
 - build_requires_systemd
 - build_requires_python-setuptools

recommend+:
- targetcli
