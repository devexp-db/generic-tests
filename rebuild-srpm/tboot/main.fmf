# Which component to build
test: COMPONENT=tboot ./runtest.sh

# List components that this test is supposed to be run for
tag:
 - build_requires_make
 - build_requires_gcc
 - build_requires_openssl
 - build_requires_zlib
 - build_requires_perl

recommend+:
- tboot
